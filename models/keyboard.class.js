/* The `class Keyboard extends MovableObject` is creating a new class called `Keyboard` that inherits
properties and methods from the `MovableObject` class. This means that the `Keyboard` class can use
all the properties and methods of the `MovableObject` class, in addition to any properties and
methods defined within the `Keyboard` class itself. */
class Keyboard extends MovableObject {
    LEFT = false;
    RIGHT = false;
    UP = false;
    DOWN = false;
    SPACE = false;
    THROW = false;
  /*   AUDIO = false; */
}